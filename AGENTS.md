# 项目协作规范

## 项目目标

构建一个面向个人写作者的本地 AI 知识库。产品先跑通「输入 → 加工 → 编译 → 问答／选题」闭环，再根据 14 天真实使用结果迭代。

## 当前阶段

当前处于 MVP 实现阶段。唯一产品基线是 `docs/PRODUCT-DESIGN.md`，真实进度以 `ROADMAP.md` 为准。

## 目录约定

- `docs/`：产品、架构和技术设计文档。
- `raw/`：不可覆盖的原始资料。
- `notes/`：单条资料加工后的原子笔记与想法笔记。
- `wiki/`：由多条笔记编译出的主题页和实体页。
- `briefs/`：可执行选题。
- `scripts/`：索引、关联、校验等机械任务脚本。
- `templates/`：各类 Markdown 模板。
- `.agents/skills/`：供 Codex 自动发现的项目级知识库工作流。
- `tests/`：机械脚本的自动化测试。

只创建当前 MVP 直接需要的目录，不提前搭建后续版本结构。

## 设计与开发约束

- 默认中文；代码、命令、变量名和文件名使用英文。
- MVP 本地优先、Markdown 优先、零运维；不引入数据库、向量库、服务器和前端 UI。
- 机械任务交给脚本，语义判断交给 AI；同一规则只保留一个事实源。
- 原始资料只读，不被加工结果覆盖；每条结论必须能追溯到原始资料或用户想法。
- 修改范围必须直接服务当前里程碑，不顺手扩功能或重构。
- 开发、修复、文档补齐或重要调研完成后，同步更新 `ROADMAP.md`。

## 完成标准

- 变更符合 `docs/PRODUCT-DESIGN.md` 的当前范围。
- 相关验收场景已经执行并记录结果。
- 未验证事项不得标记为已完成。
- 不确定信息写为「待确认」，不得自行补成事实。
